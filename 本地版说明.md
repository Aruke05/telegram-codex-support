# AI 客服本地版

## 启动与关闭

- 双击 `/Users/mercury/IdeaProjects/ai客服/AI客服.app`，管理后台会直接显示在程序窗口中。
- 本地地址固定为 `http://127.0.0.1:3210`。
- 关闭窗口或退出程序会同时终止其 Node.js 子进程；正常退出超过 5 秒时会强制结束该子进程。
- 每次启动都会确认服务器客服已经停止，避免两套程序同时使用同一 Telegram 客服账号。

## 代码版本

- 本地客服代码以提交 `450a0fb517905252729b6a6bacd5e670c1482141`（`修复：按运营接口权限判断技术升级`）为基础。
- 本地维护分支是 `telegram-ai-support-local`，源码位于 `/Users/mercury/IdeaProjects/ai客服-source`。
- 后续 2026-08-27 至 2026-08-30 的客服状态机与固定话术提交没有合入本地版。

## 数据与代码库

- 原始服务器 SQLite 一致性快照保存在 `data/server-snapshot/support.sqlite`，实际运行库位于 `data/runtime/support.sqlite`。
- 运行库保留服务器快照数据以及本地接收的后续 Telegram 记录；替换程序时不会覆盖运行库。
- Telegram 个人账号断线后先由客户端自动重连；连接恢复或重新建立连接后会执行遗漏更新补取。
- AI 客服读取的前后端 Git 仓库独立存放于 `/Users/mercury/IdeaProjects/ai客服项目代码`，不访问原四方支付工作目录。
- 是否复用现有工作树、选择已有 worktree 或新建隔离 worktree，由回答模型结合现场 Git 状态判断；程序不使用关键词、业务分支清单或占用锁代替模型裁决。

本目录包含客服账号、项目服务器和数据库连接资料，不要提交到 Git、上传网盘或发给其他人。
